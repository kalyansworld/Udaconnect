#Overall segregated 3 domains(Person, Location & Connection) into separate individual microservices.
#Front end needs to call all 3 microservices individually.
#POST API's are meant to insert data into DB, so carrying out the operation through Kafka
#Kafka Consumer receives the DB insertion call & invokes GRPC through which data got persisted into DB.